import 'package:flutter/material.dart';

//page for actually displaying the team stuff
//at the most basic level, it will not have chaned data
//we may manually change ouselves for now

class TeamInfo extends StatelessWidget {
  const TeamInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          //Welcome Team
          //Here are the participants
          //Here is your teamID
        ],

      ),
    );
  }
}
